PORT=4010
NODE_ENV=production

# PostgreSQL (local on phone-b)
DB_TYPE=postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=password123
DB_NAME=agents
DB_SYNCHRONIZE=true

# RabbitMQ — proot Debian on phone-b (127.0.0.1:5672)
# See docs/RABBITMQ-TERMUX.md and docs/PHONE-B-SETUP.md
RABBIT_MQ_URI=amqp://rmuser:password123@127.0.0.1:5672
# Optional PC fallback only:
# RABBIT_MQ_URI=amqp://rmuser:password123@100.98.162.107:5672

# Default profile: stub (phase 7–8). For live Gemini see agents-prod.phone-b.env.live.example (phase 10).
# LLM — stub for phone lab (no Gemini on device)
LLM_STUB=true
GEMINI_API_KEY=

# Memory limit for health/ready (bytes) — 240 MB
HEAP_MAX_SIZE=251658240

# Stubs — phase 7 smoke uses test_durable_workflow only
GSC_STUB=true
SERP_STUB=true
SEO_MONITOR_CRON_ENABLED=false
SEO_MONITOR_SITE_URL=https://ezrababait.co.il/

# Phase 13 — api-content-prod (current: phone-a :4004; on phone-b use http://127.0.0.1:4004)
CONTENT_INTERNAL_URL=http://100.120.187.10:4004
INTERNAL_SERVICE_TOKEN=phone-lab-internal-token

COMPETITOR_CRAWL_STUB=true
