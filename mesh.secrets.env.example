# Copy to mesh.secrets.env — never commit (gitignored).
# Termux SSH password (set via `passwd` on each phone).
# Optional: shared token for content bridge smoke (must match phone-b .env files).

PHONE_SSH_PASSWORD=
INTERNAL_SERVICE_TOKEN=phone-lab-internal-token

# Phase 10 — live Gemini on phone-b (required for smoke:phase10)
GEMINI_API_KEY=
